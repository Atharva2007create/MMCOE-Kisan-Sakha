# Kisan Sakha · MMCOE — production dependencies
#
# Environment (required for AI):
#   GOOGLE_API_KEY — from https://aistudio.google.com/apikey
# Optional:
#   DATA_GOV_IN_API_KEY — data.gov.in (Maharashtra mandi merge, daily cache)
#
# Streamlit Cloud: add the same keys under Settings → Secrets.

streamlit>=1.28.0
google-generativeai>=0.8.0
pandas>=2.0.0
plotly>=5.18.0
requests>=2.31.0
